[project]
name = "mcp-movie-db-server"
version = "0.1.0"
description = "Movie DB MCP server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp",
    "pydantic",
    "httpx",
    "starlette",
    "google-cloud-aiplatform",
]